import BlockSettings from "../common/BlockSettings"

export const PlaceholderBlockSettings = ({block}) => {

  return (
    <BlockSettings block={block} />
  )
}

export default PlaceholderBlockSettings