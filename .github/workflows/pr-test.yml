name: PR Test

on:
  push:
    branches: [ 15-frontend-test-cases ]
  pull_request:
    branches: [ main ]

jobs:
  trigger_playwright_test:
    uses: ./.github/workflows/playwright-test.yml 

  pr-comment:
    needs: [trigger_playwright_test]
    runs-on: ubuntu-latest

    steps:
      - name: Download Test Report
        uses: actions/download-artifact@v4
        with:
          name: test-results

      - name: Read Test Result and Coverage
        id: testinfo
        run: |
          status=$(jq -r '.status' .last-run.json)
          failed_count=$(jq '.failedTests | length' .last-run.json)
          coverage=$(jq -r '.overallCoverage' coverage/coverage-report.json)

          echo "status=$status" >> $GITHUB_OUTPUT
          echo "failed_count=$failed_count" >> $GITHUB_OUTPUT
          echo "coverage=$coverage" >> $GITHUB_OUTPUT

      - name: Comment To PR
        run: |
          if [ "${{ steps.testinfo.outputs.status }}" == "passed" ]; then
            ICON="✅"
            SUMMARY="${ICON} All tests passed.\n"
          else
            ICON="❌"
            SUMMARY="${ICON} ${{ steps.testinfo.outputs.failed_count }} tests failed.\n"
          fi

          COVERAGE="📊 Coverage: ${{ steps.testinfo.outputs.coverage }}"
          JOB_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"

          COMMENT="${SUMMARY}${COVERAGE}\n🔗 [View Full Report](${JOB_URL})"

          # curl -X POST -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
          #   -H "Content-Type: application/json" \
          #   -d "{\"body\": \"$COMMENT\"}" \
          #   "https://api.github.com/repos/${{ github.repository }}/issues/${{ github.event.pull_request.number }}/comments"

          curl -X POST -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -H "Content-Type: application/json" \
            -d "{\"body\": \"$COMMENT\"}" \
            "https://api.github.com/repos/${{ github.repository }}/issues/150/comments"    
